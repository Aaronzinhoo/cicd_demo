version: 2.1

orbs:
  docker: circleci/docker@1.3.0

workflows:
  build-deploy:
    jobs:
      - docker/publish:
          image: aaronzinhoo/$CIRCLE_PROJECT_REPONAME
          tag: $CIRCLE_SHA1
          tag: latest
          dockerfile: Dockerfile
          deploy: true
          filters:
            branches:
              only:
                - master
